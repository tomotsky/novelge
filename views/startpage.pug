doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title ノベルゲー
  body
    p 名前を入力してね。
    form(method="post")
      span 名前
      input(type="text" name="name")
      button(type="submit") OK
